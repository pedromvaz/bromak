package com.bromakgame

class Champion extends Creature {

	// TODO: If we create the Player class, fix this
	// TODO: Can't get belongsTo to work, says user is null upon saving
	//static belongsTo = [ user: User ]
	
    static constraints = {
    }
}
